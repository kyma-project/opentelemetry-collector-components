type: dummy

status:
  class: receiver
  stability:
    alpha: [metrics]
  distributions: [kyma]
  codeowners:
    active: [kyma-project/observability]

resource_attributes:
  k8s.cluster.name:
    value: test-cluster

attributes:
  host:
    description: the current hostname

metrics:
  dummy:
    description: a dummy gauge
    attributes: [host]
